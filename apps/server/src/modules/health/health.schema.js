// Health endpoints intentionally have no request payload. Response schemas will
// be registered with the API documentation layer when Swagger is enabled.
export const healthSchemas = Object.freeze({});
