/**
 * Health response shapes are documented with JSDoc so JavaScript developers
 * receive editor guidance without introducing TypeScript into the project.
 *
 * @typedef {{status: 'ok', service: string, uptimeSeconds: number, timestamp: string}} LivenessResponse
 * @typedef {{status: 'ready', database: 'available', timestamp: string}} ReadinessResponse
 */

export {};
