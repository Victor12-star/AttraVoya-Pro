import { AdminSectionPage } from '../admin-section-page.jsx';

export default function EmergencyPage() {
  return <AdminSectionPage title="Emergency records" description="Only authoritative, verified emergency records will be managed here. AI-generated emergency numbers are never accepted." />;
}
