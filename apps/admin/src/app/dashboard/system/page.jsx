import { AdminSectionPage } from '../admin-section-page.jsx';

export default function SystemPage() {
  return (
    <AdminSectionPage
      title="System"
      description="Operational system controls will be added here only when they have explicit authorization, validation, and audit coverage."
    />
  );
}
