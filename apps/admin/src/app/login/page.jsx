export default function AdminLoginPage() {
  return (
    <main className="admin-main">
      <section className="admin-card">
        <h1>Admin sign in</h1>
        <p className="admin-muted">
          The privileged Admin sign-in interface will be connected to the existing secure authentication API before this workspace is released.
        </p>
      </section>
    </main>
  );
}
