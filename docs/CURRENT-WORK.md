# AttraVoya Pro — Current Work Handoff

Last updated: 2026-09-05

This file is the engineering handoff for continuing AttraVoya Pro without relying on chat history.

## Repository and workflow

- GitHub: `Victor12-star/AttraVoya-Pro`
- Working integration branch: `develop`
- `main` is not the day-to-day development branch.
- Latest verified merged checkpoint before the current PR: Phase 7K merge commit `2a6706dc9df52bcf35ddd844ac0a3a6fd2d9674f`.
- Phase 7K post-merge `develop` CI `#166` passed all five workflow jobs.
- Current pull request: `#12` from `feature/phase-7l-destination-currency` into `develop`.

Every vertical slice must follow this sequence:

1. Create a feature branch from the latest verified `develop` commit.
2. Implement one coherent slice only.
3. Open a pull request into `develop`.
4. Require all GitHub CI jobs to pass on the exact final PR head.
5. Merge only after the complete gate is green.
6. Verify the resulting `develop` push CI is also fully green.
7. Only then start the next slice.

The standard CI gate includes JavaScript checks, translation parity, provider smoke tests, ESLint, unit/integration tests, normal Prettier formatting, PostgreSQL/Prisma verification, dependency/secret checks, production builds, and live-provider checks when credentials are available.

Never weaken CI to make a slice pass, and never claim a provider was live-verified when its required API key was not configured.

## Product and architecture foundation

The project is a JavaScript monorepo with a Next.js customer website, Next.js Admin app, Expo/React Native mobile app, Node.js/Fastify API, PostgreSQL/Prisma, Zod validation, shared API client, localization/global country system, authentication/session foundation, Guest/Free/Premium/Admin/Super Admin separation, budget-first trip-planning domain foundation, and verified-safety architecture.

Provider adapters already implemented include:

- Open-Meteo weather
- Frankfurter currency
- LibreTranslate translation
- Geoapify places/accommodation/geocoding/routing
- Ticketmaster events
- NewsData travel news
- Pexels destination imagery
- Resend transactional email

Provider secrets stay server-side. Never invent live travel prices, availability, emergency facts, ratings, opening times, exchange rates, safety claims, or other provider data.

## Phase 7 destination progress

Completed and merged destination slices:

- Phase 7A — Global destination search API
- Phase 7B — Customer destination search UI
- Phase 7C — Destination page foundation
- Phase 7D — Attractions discovery
- Phase 7E — Restaurants discovery
- Phase 7F — Beaches discovery
- Phase 7G — Shopping discovery
- Phase 7H — Accommodation discovery
- Phase 7I — Family destination discovery
- Phase 7J — Nearby destination discovery

Important verified checkpoints from the later slices:

- Phase 7G merged at `ff40f33f257ba2e2cb8688d65912e01aebd76403`; PR CI `#133` and post-merge CI `#134` green.
- Phase 7H merged at `05162952c277388fc143b4d45a2d8e1d94758294`; PR CI `#140` and post-merge CI `#141` green.
- Phase 7I merged at `cf05786c12279bf884e99312539d9f116f0de92d`; PR CI `#148` and post-merge CI `#149` green.
- Phase 7J merged at `3f4dc70cf173d954d92a7a002813a1124eaae552`; PR CI `#153` and post-merge CI `#154` green.

### Phase 7K — Verified Safety destination foundation

Completed and merged through PR `#11` into `develop` at:

`2a6706dc9df52bcf35ddd844ac0a3a6fd2d9674f`

Implemented:

- real `/destinations/[slug]/safety` verified-emergency experience
- public read-only `/api/v1/emergency?countryCode=XX` with strict ISO2 validation
- existing `EmergencyRecord` provenance model reused rather than a parallel safety store
- only country-matching, `VERIFIED`, published, country-wide, source-backed records with verification metadata can cross the public contract
- service-level defense in depth revalidates required fields, authoritative HTTP/HTTPS source URL and `lastVerifiedAt`
- no emergency number is seeded, guessed, inferred from locale, or generated by AI
- destination Safety UI rejects mismatched response-country context
- genuine service label, phone number, safe `tel:` action, official source and last-verified metadata only
- honest empty/error/retry states
- basic emergency information remains public and subscription-independent
- no fabricated crime score, neighborhood safety, political risk, medical access or travel advisory claims
- all 18 locales, RTL, responsive and reduced-motion behavior
- focused server and web tests

Final PR head CI `#165` passed all five jobs. Phase 7K merged at `2a6706dc9df52bcf35ddd844ac0a3a6fd2d9674f`, and post-merge `develop` CI `#166` also passed all five jobs.

### Phase 7L — Destination currency and exchange

Current implementation is in PR `#12` from `feature/phase-7l-destination-currency` into `develop`.

The branch starts from the verified Phase 7K merge commit:

`2a6706dc9df52bcf35ddd844ac0a3a6fd2d9674f`

Implemented:

- replaced `/destinations/[slug]/currency` unavailable shell with a real destination-aware currency experience
- reused the validated destination route/context
- destination currency/currencies come only from AttraVoya's country reference API; currency is never inferred from UI language or locale
- country currencies retain real reference metadata such as code, display name, symbol and primary flag
- primary country currency is attempted first; for a genuine multi-currency country another reference currency may be tried only when the configured rate provider cannot serve the first one
- reused the existing provider-neutral `/api/v1/currency/rates` and `/api/v1/currency/convert` backend contracts
- browser code never calls Frankfurter directly
- Frankfurter results are accepted only when the expected base/from/to currencies match, the provider is present, the values are finite, and the backend marks the result `approximate: true`
- malformed or mismatched conversion responses are rejected rather than displayed
- saved traveller currency preference is used only when the rate provider supports it
- changing the source currency updates only the existing non-sensitive browser currency preference
- conversion input is bounded to the server contract: amount greater than 0 and at most 100,000,000
- customer UI clearly labels rates as indicative reference rates
- explicit disclaimer states that banks, card issuers, cash exchange offices and payment providers may apply different rates and fees
- no bank rate, card rate, cash exchange rate, spread, fee, ATM cost, merchant behavior or payment availability is fabricated
- honest loading, destination-reference-empty, provider-unavailable, invalid-input, conversion-error and retry states
- all 18 supported locales
- RTL-compatible, responsive and reduced-motion-aware styling
- focused Currency UI tests

CI findings fixed without weakening the gate:

- initial CI `#167` caught one strict-JavaScript inference issue in the async conversion state; an explicit `Promise<ConversionState>` JSDoc return contract fixed it
- CI `#168` then passed JavaScript, translations, provider smoke, ESLint, all tests, database, dependency/secret checks, production builds and live-provider checks; only Prettier remained
- a temporary diagnostic changed only the root `format:check` script so CI could run Prettier on the three new Currency JavaScript files, print the exact diff, restore those files in the runner and intentionally fail
- exact Prettier output was applied to `currency-page-copy.js`, `currency-page.jsx` and `currency-page.test.jsx`
- the temporary diagnostic was completely removed; root `package.json` is restored byte-for-byte to its original blob `af4b2abbc4b5b1887f9a6293cd1a411649a69f7c` and is not part of the final PR diff

Clean Phase 7L code checkpoint before this handoff update:

`ac7fa73a89a7e19a5596f6357f17c5d23fbff75c`

Clean code CI `#173` passed all five workflow jobs, including strict JavaScript, all translation checks, provider smoke tests, ESLint, all unit/integration tests, normal repository-wide `prettier --check .`, PostgreSQL/Prisma verification, dependency/secret checks, production builds and live-provider checks.

This documentation update creates a newer PR head. Do not merge PR `#12` based only on CI `#173`; require a new complete green CI run on the exact final head containing this handoff update.

## Live-provider verification status

The live-provider CI currently verifies these real network paths:

- Open-Meteo
- Frankfurter
- CI-hosted LibreTranslate

Keyed provider checks are skipped when their GitHub Actions secrets are not configured, including:

- Geoapify (`GEOAPIFY_API_KEY`)
- Ticketmaster (`TICKETMASTER_API_KEY`)
- NewsData (`NEWSDATA_API_KEY`)
- Pexels (`PEXELS_API_KEY`)

Therefore Geoapify-backed destination slices are covered by shared-category, adapter/API/UI tests and production builds, but do not claim a real Geoapify network request was verified by CI unless its key is configured and the live check actually runs.

Frankfurter is exercised by the live-provider CI, but customer exchange results must still be described as indicative reference rates rather than guaranteed bank, card, cash or merchant rates.

Safety is database-backed verified reference data, not a third-party live-provider lookup. A successful Safety test verifies the filtering/provenance contract; it does not mean verified emergency records exist for every country.

## Immediate next engineering step

Finish Phase 7L safely:

1. Require complete green GitHub CI on the exact final head of PR `#12` after this handoff update.
2. Fix any real failure without weakening CI.
3. Confirm PR `#12` is mergeable and still points to that exact verified head.
4. Merge PR `#12` into `develop` only after all five jobs are green.
5. Verify the resulting `develop` push CI is fully green.
6. Only then create the next feature branch from that exact verified merge commit.

## Recommended next slice

### Phase 7M — Destination Language foundation

The destination route `/destinations/[slug]/language` is still an unavailable shell.

The project already has two useful foundations that should be reused:

- country reference data includes ordered country-language relationships with `isOfficial`, `isCommon`, rank, language code, name, native name, direction and UI-support metadata
- the existing provider-neutral translation API exposes text translation plus the provider's supported-language list, backed by LibreTranslate through the server

The separate `apps/server/src/modules/phrasebook` module currently consists of empty placeholder files. Do not pretend a phrasebook backend already exists and do not fold an unverified phrasebook data model into the first Language slice.

Suggested Phase 7M scope:

1. Replace the destination Language unavailable shell with a destination-aware page using validated destination country context.
2. Resolve official/common destination languages only from AttraVoya's country-language reference data; never infer official language from UI locale or machine translation support.
3. Clearly distinguish `official` and `common` flags already stored in reference data from UI-language support.
4. Show genuine language code, localized/display name, native name and writing direction where available.
5. Add a small optional machine-translation tool only through AttraVoya's existing translation API; browser code must not call LibreTranslate directly.
6. Retrieve the provider-supported translation-language list and disable or honestly mark unsupported language pairs instead of inventing translations.
7. Label translation output clearly as machine translation, not an official phrase or verified local expression.
8. Do not add pronunciation, transliteration, dialect prevalence, phrase correctness, cultural claims or audio unless a real source/provider supports them.
9. Keep a future phrasebook as a separate later slice after its authoritative content/source and backend contract are designed.
10. Preserve all 18 UI locales, RTL, accessibility, responsive behavior and reduced motion; add focused tests and require the full CI gate.

## Product constraints that must not be forgotten

- Budget-first trip planning is a core differentiator.
- Cheapest room is not necessarily cheapest total trip.
- Family travel must use children's ages: `0–3`, `4–8`, `9–12`, `13–17`.
- Basic emergency/safety functionality is never paywalled.
- Official emergency numbers must come from authoritative verified data, never AI.
- Premium never grants Admin permissions.
- Frontend visibility is never an authorization boundary.
- Use JavaScript only unless the product owner explicitly approves TypeScript.
- Use Lucide icons consistently.
- Avoid generic AI-template visuals, fake testimonials, fake statistics and fake live data.
- Whole-app language support and country/language/currency separation must remain intact.
- All new async UI features need loading, success, empty, error and retry behavior where useful.
- Every visible interactive control must genuinely work.
- Do not call a phase complete unless the relevant runtime/CI tests actually passed.

## How to resume in a new chat

Tell ChatGPT:

> Continue AttraVoya Pro from `docs/CURRENT-WORK.md` in GitHub repository `Victor12-star/AttraVoya-Pro`. Read that file first, inspect `develop`, then continue the next unfinished phase. Keep the rule that every slice must pass GitHub CI before proceeding.

The repository and this handoff are the source of truth if chat memory and Git history ever disagree.
