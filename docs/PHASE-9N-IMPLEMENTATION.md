# Phase 9N implementation contract

Accommodation presets expand to provider-neutral `AccommodationType` values. Manual lodging checkbox changes become a custom selection. Optional nightly and total accommodation caps are persisted through the existing validated `TravelStayPreference` fields, and room/stay type is persisted through `StayUnitType`.
